########################################
# HTTP → HTTPS
########################################
server {
    listen 80;
    server_name {{ nginx_server_name }};

    return 301 https://$host$request_uri;
}

########################################
# HTTPS (stubbed for now)
########################################
server {
    listen 443 ssl;
    server_name {{ nginx_server_name }};

    # --- SSL (adjust paths) ---
    {% if nginx_letsencrypt | default(false) %}
    ssl_certificate     /etc/letsencrypt/live/{{ nginx_server_name }}/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/{{ nginx_server_name }}/privkey.pem;
    {% else %}
    ssl_certificate     /etc/nginx/ssl/fullchain.pem;
    ssl_certificate_key /etc/nginx/ssl/privkey.pem;
    {% endif %}

    ####################################
    # Redirect exact paths → trailing /
    ####################################
    location = /app1 { return 301 /app1/; }
    location = /app2 { return 301 /app2/; }

    ####################################
    # STUBBED RESPONSES (current)
    ####################################
    location /app1/ {
        default_type application/json;
        return 200 '{"status":"ok","app":1}';

        # ---- READY FOR REAL APP ----
        # proxy_pass http://127.0.0.1:5000/;
        # proxy_set_header Host $host;
        # proxy_set_header X-Real-IP $remote_addr;
        # proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        # proxy_set_header X-Forwarded-Proto $scheme;
        # proxy_http_version 1.1;
        # proxy_set_header Upgrade $http_upgrade;
        # proxy_set_header Connection "upgrade";
    }

    location /app2/ {
        default_type application/json;
        return 200 '{"status":"ok","app":2}';

        # ---- READY FOR REAL APP ----
        # proxy_pass http://127.0.0.1:5001/;
        # proxy_set_header Host $host;
        # proxy_set_header X-Real-IP $remote_addr;
        # proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        # proxy_set_header X-Forwarded-Proto $scheme;
        # proxy_http_version 1.1;
        # proxy_set_header Upgrade $http_upgrade;
        # proxy_set_header Connection "upgrade";
    }
}
